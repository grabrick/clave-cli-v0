target/
# Артефакты ранов движка: дефолтный OUT_DIR — .clave/ (legacy — .ai-runs/).
.clave/
.ai-runs/
.DS_Store
# Каталог worktree'ов харнесса — не должен делать рабочее дерево грязным.
.claude/worktrees/
.venv/
**/.venv/

